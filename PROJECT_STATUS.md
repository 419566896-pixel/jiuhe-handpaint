# 久合手绘喷绘工艺 — 项目进度

> 最后更新：2026-06-10

---

## 📂 项目结构

```
~/Projects/jiuhe-handpaint/
├── app/
│   ├── layout.tsx                  # 根布局 + SEO metadata
│   ├── page.tsx                    # 首页（全量）
│   ├── globals.css                 # 全局样式
│   ├── robots.ts                   # SEO robots
│   ├── sitemap.ts                  # SEO sitemap
│   ├── components/
│   │   ├── ContactForm.tsx         # 预约表单
│   │   ├── HeroCarousel.tsx        # 首屏背景轮播（性能优化：只渲染3张）
│   │   └── GalleryCarousel.tsx     # 作品展示交叉淡入淡出轮播
│   └── api/contact/route.ts        # 表单提交 API
├── public/images/                  # 49 张图片（已对齐）
├── next.config.ts
├── .gitignore
└── PROJECT_STATUS.md
```

---

## 🎯 已完成功能

### 1. 首屏 Hero Section
- 全屏黑底 + HeroCarousel 自动轮播背景（2秒间隔）
- 主标题："久合手绘喷绘工艺"（超大字号 `clamp(4rem, 14vw, 16rem)`，自适应）
- 副标题："手绘的温度，让每件单品都独一无二 · 广州批发市场都在找的手绘厂"
- 卖点文字："20年画师团队 | 3天打样 | 服务白马/红棉/十三行千家客户"
- 两个 CTA 按钮："看作品案例" + "立即预约打样"
- **预约打样表单叠加在左下角**（`w-[60rem]`，半透明背景 + 毛玻璃效果）
- **联系方式固定悬停在右下角**（`fixed` 定位，黄色大字，手机/微信 + 地址 + 工作时间一行显示）

### 2. 信任背书 Section
- 6 个广州批发市场标签（中大/国际轻纺城/十三行/站西金宝/红棉/沙河）
- 3 大业务数据：20+年经验、5000+客户、100000+年产量

### 3. 作品展示 Gallery
- GalleryCarousel：交叉淡入淡出轮播
- 49 张图片，4.5 秒切换一组，每组 6 张（桌面端）/ 3 张（移动端）
- 前一组渐渐消失（opacity 1→0），后一组同时渐渐出现（opacity 0→1）
- 使用 CSS Grid `grid-area: stack` 叠加两组实现交叉淡入淡出
- 底部指示点导航可点击跳转

### 4. 三大品类 Section
- 服装手绘（T恤/卫衣/牛仔/民族服饰）
- 鞋履喷绘（帆布鞋/板鞋/小白鞋/运动鞋）
- 箱包手绘（帆布包/手提包/背包/行李箱）
- 每个品类包含 4 个特性（已删除起订和周期信息）

### 5. 四大理由 Section
- 手绘的温度 / 交期靠谱 / 灵活接单 / 技术过硬

### 6. 四步流程 Section
- 沟通需求 → 报价打样 → 确认生产 → 验货交付

### 7. 客户评价 Section
- 4 位客户真实评价

### 8. Footer
- 版权信息 + 品牌描述

---

## 🎨 视觉设计
- **主题**：奢华暗黑色调（bg-gray-900 / bg-black）
- **主色**：黄色 (#EAB308) + 橙色 (#F97316) 作为强调
- **首屏**：主标题超大字号 + 联系方式黄色高亮
- **响应式**：grid-cols-2 → md:grid-cols-3/4/6
- **首屏文字位置**：整体上移 13cm（`marginTop: '-13cm'`）

---

## 📸 图片资源
- public/images/ 目录共 **49 张有效图片**（排除文件名含空格的 3 张）
- HeroCarousel 引用 49 张（运行时只渲染当前+相邻共 3 张，优化性能）
- GalleryCarousel 引用 49 张（CSS Grid 叠加实现交叉淡入淡出）
- 已删除：workshop-4.jpg

---

## ✅ 本次会话完成的工作（2026-06-10）

### 功能改造
1. **GalleryCarousel 轮播改造**
   - 从分组切换 → 连续滚动 → 交叉淡入淡出轮播
   - 最终方案：CSS Grid `grid-area: stack` 叠加 + opacity 过渡
   - 切换间隔：4.5 秒，淡入淡出 0.8 秒

2. **首屏 Hero 改造**
   - 主标题改为"久合手绘喷绘工艺"
   - 字号放大（`clamp(4rem, 14vw, 16rem)`）
   - 去掉宽度限制，撑满整屏，不换行
   - 整体上移 13cm

3. **预约打样表单叠加首屏左下角**
   - 半透明背景 + 毛玻璃效果
   - 宽度 `w-[60rem]`

4. **联系方式固定悬停右下角**
   - `fixed` 定位，始终可见
   - 黄色大字，一行显示

5. **删除底部独立的预约/联系栏目**

6. **删除所有"起订"和"周期"相关内容**
   - 首屏卖点、信任背书、品类特征、品类卡片底部、灵活接单描述

### 性能优化
7. **HeroCarousel 性能优化**
   - 从渲染全部 49 张 → 只渲染当前+相邻 3 张
   - 减少 DOM 节点和内存占用

8. **图片懒加载**
   - GalleryCarousel 使用 `loading="lazy"`
   - HeroCarousel 首张 priority，其余 lazy

### 项目管理
9. **Git 初始化 + 2 次提交**
   - `feat: 久合手绘喷绘工艺官网 - 首页全量完成`
   - `perf: HeroCarousel 性能优化 + 更新进度文件`

10. **SEO 完善**
    - layout.tsx：标题/描述/关键词/OpenGraph
    - robots.ts + sitemap.ts

11. **图片列表对齐**
    - 两个组件统一使用 49 张图片
    - 排除文件名含空格的 3 张

---

## ⚠️ 待办事项

### 中优先级
1. **表单后端集成** — 当前只 console.log，需对接邮件/微信通知服务
2. **百度/Google 站长验证** — layout.tsx 中 verification 字段待填

### 低优先级
3. **图片压缩/WebP 格式** — 大图加载需考虑压缩
4. **多页面扩展** — 关于我们、服务详情等子页面

---

## 📞 联系方式
- 手机/微信：13168873754
- 地址：广州市白云区石井大冈村大园街12号
- 工作时间：周一至周六 9:00-18:00
